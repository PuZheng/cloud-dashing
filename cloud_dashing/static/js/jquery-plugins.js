define(['jquery'], function ($) {
	// Only once jquery has loaded, load the jQuery plugins
	require(['jquery.plot.crosshair', 'jquery.plot.time', 'jquery.plot.tooltip', 'jquery.plot.symbol'], function () {
		// Do nothing
	});
});
